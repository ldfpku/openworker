# 手册

四本，按角色分。找不到答案再往深处的 [gemini-relay/docs/](../../gemini-relay/docs/) 翻。

| 手册 | 读者 | 什么时候读 |
| --- | --- | --- |
| [01-管理员初始化手册](./01-管理员初始化手册.md) | 搭这套东西的人 | 从零部署；接手别人搭好的；要打包发版 |
| [02-运维手册](./02-运维手册.md) | 日常维护的人 | 加人删人、调额度、查用量、有人报障、升级 |
| [03-用户手册](./03-用户手册.md) | 所有使用者 | 拿到安装包之后 |
| [04-Cloudflare-AI-Gateway](./04-Cloudflare-AI-Gateway.md) | 管理员 + 使用者 | 想用 Gemini 以外的模型（GPT / Claude / Grok / DeepSeek…） |

## 一句话说清这套东西

同事装 OpenWorker 桌面应用 → 所有 Gemini 请求走公司的 Cloudflare Worker 中转
（因为 Google 接口在国内不通）→ 中转用 Cloudflare 一次性验证码确认「是名单里的人」，
再把请求连同**他那把 Gemini API key**（管理员从一个公司 Google 账号签发、以他名字命名）
转给 Google，同时按邮箱记账和限额。

**登录和 key 是两件事**：登录说明你是谁，key 才是去调 Google 的凭证。同事两样都要有，
两样都由管理员给。中转自己一把 key 都不存。

**Gemini 以外的模型**（GPT、Claude、Grok、DeepSeek…）不走这条路，走 Cloudflare AI
Gateway：同事点一次登录就行——网关地址内置在应用里，没有密钥要发、没有东西要填；
费用从账号的预付额度里扣，我们一行转发代码都不用写。
见 [04-Cloudflare-AI-Gateway](./04-Cloudflare-AI-Gateway.md)。两条路刻意不重叠——
Gemini 只走中转，别家只走网关。

## 现状

- 代码、名单、文档都已完成；桌面端发版流水线（含自动更新签名）已配好。
- **中转 v3 已于 2026-08-23 部署上线**，Zero Trust 验证码登录已打通。
- **顺序锁死：先部署 Worker，再发布桌面端。** v3 的应用打到 v2 的中转上完全不能用
  （登录接口在 v2 上是 404）；反过来 v3 一上线，还在用 v2 的客户端立刻 401。
  以后每次升级都照这个顺序，管理员自己那台先升级。
- 发版步骤见
  [管理员手册第 6 节](./01-管理员初始化手册.md#6--发版让-github-上有可下载的安装包)。
