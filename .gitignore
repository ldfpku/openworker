.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
build/
dist/
.coverage

# Local secrets (live-smoke BYO keys) — never committed
.env
# The real relay roster: colleagues' names and personal mailboxes. This fork is PUBLIC,
# so the filled-in roster stays on the admin's machine — only roster.example.csv ships.
gemini-relay/scripts/roster.csv
.research/proxy-guard/README.pdf
# Per-colleague AI Gateway tokens issued by issue_aigw_tokens.py. Live credentials
# plus the staff list, in a public fork — the script refuses to write if this is missing.
gemini-relay/scripts/aigw-tokens.env
